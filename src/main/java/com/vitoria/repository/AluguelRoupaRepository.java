package com.vitoria.repository;

import com.vitoria.model.AluguelRoupa;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AluguelRoupaRepository extends JpaRepository<AluguelRoupa, Long> {
}
